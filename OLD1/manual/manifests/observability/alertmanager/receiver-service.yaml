apiVersion: v1
kind: Service
metadata:
  name: alertmanager-receiver
  namespace: monitoring
  labels:
    app: alertmanager-receiver
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: alertmanager-receiver
