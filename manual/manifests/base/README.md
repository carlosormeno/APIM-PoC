# Base manifests (manual)

Incluye recursos base compartidos.

- `cert-manager/`: CRDs y despliegue manual de cert-manager (manifest oficial).
- `cert-issuer.yaml`: ClusterIssuer self-signed (para PoC).
